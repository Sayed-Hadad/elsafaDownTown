# دليل التشغيل السريع - Safa Downtown Halls Booking System
# Quick Start Guide

## المتطلبات الأساسية / System Requirements

- Python 3.8 أو أحدث / Python 3.8 or newer
- pip (مدير حزم Python)
- Windows / macOS / Linux

## خطوات التثبيت والتشغيل / Installation & Setup

### 1. فتح سطر الأوامر / Open Command Prompt

اذهب إلى مجلد المشروع واحفظه كـ:
```
cd C:\Users\Sayed El-haddad\Desktop\elsafa
```

### 2. إنشاء بيئة افتراضية (اختياري ولكن موصى به)
```bash
# Windows
python -m venv venv
venv\Scripts\activate

# macOS/Linux
python3 -m venv venv
source venv/bin/activate
```

### 3. تثبيت المكتبات / Install Dependencies
```bash
pip install -r requirements.txt
```

### 4. تشغيل التطبيق / Run the Application
```bash
python run.py
```

سترى رسالة مثل:
```
 * Running on http://127.0.0.1:5000
```

### 5. فتح التطبيق / Open in Browser

اذهب إلى: **http://localhost:5000**

---

## بيانات تسجيل الدخول / Login Credentials

تطبيق بها **حسابات تجريبية مدمجة**:

### حساب المدير / Admin Account
```
اسم المستخدم: admin
كلمة المرور: admin123
```

### حسابات الموظفين / Staff Accounts
```
اسم المستخدم: staff1
كلمة المرور: staff123

اسم المستخدم: staff2
كلمة المرور: staff123
```

---

## الميزات الأساسية / Key Features

### للمدير Admin:
✓ لوحة تحكم مع إحصائيات
✓ إدارة القاعات (إضافة، تعديل، حذف)
✓ عرض جميع الحجوزات
✓ تصريح أو رفض الحجوزات
✓ إدارة الموظفين
✓ عرض سجلات الأنشطة
✓ التقويم الكامل

### للموظف Staff:
✓ لوحة تحكم شخصية
✓ إضافة حجوزات جديدة
✓ عرض الحجوزات الخاصة بهم
✓ تعديل الحجوزات
✓ عرض القاعات المتاحة
✓ تحديثات في الوقت الفعلي

---

## هيكل المشروع / Project Structure

```
elsafa/
├── app/                    # التطبيق الرئيسي
│   ├── __init__.py        # تهيئة Flask
│   ├── models.py          # نماذج قاعدة البيانات
│   ├── utils.py           # دوال مساعدة
│   ├── routes/            # مسارات التطبيق
│   │   ├── auth.py        # التحقق والدخول
│   │   ├── dashboard.py   # لوحات التحكم
│   │   ├── halls.py       # إدارة القاعات
│   │   ├── bookings.py    # إدارة الحجوزات
│   │   ├── api.py         # واجهات برمجية
│   │   └── admin.py       # لوحة المدير
│   ├── templates/         # قوالب HTML
│   │   ├── base.html      # القالب الأساسي
│   │   ├── auth/          # قوالب الدخول
│   │   ├── dashboard/     # قوالب لوحات التحكم
│   │   ├── halls/         # قوالب القاعات
│   │   ├── bookings/      # قوالب الحجوزات
│   │   └── admin/         # قوالب الإدارة
│   └── static/            # ملفات ثابتة
│       ├── css/           # أنماط CSS
│       ├── js/            # سكريبتات JavaScript
│       └── images/        # صور
├── run.py                 # ملف التشغيل الرئيسي
├── requirements.txt       # المكتبات المطلوبة
├── README.md              # التوثيق الكامل
└── QUICKSTART.md          # هذا الملف

```

---

## قاعدة البيانات / Database

**SQLite** - قاعدة بيانات خفيفة الوزن محفوظة في:
```
elsafa_halls.db
```

يتم إنشاؤها تلقائياً عند التشغيل الأول.

---

## المشاكل الشائعة وحلولها / Troubleshooting

### المشكلة: "Module not found"
**الحل:**
```bash
pip install -r requirements.txt
```

### المشكلة: "Port 5000 already in use"
**الحل:** غيّر المنفذ في `run.py`:
```python
app.run(debug=True, port=5001)  # استخدم 5001 بدلاً من 5000
```

### المشكلة: قاعدة البيانات معطوبة
**الحل:** احذف الملف وابدأ من جديد:
```bash
# Windows
del elsafa_halls.db
python run.py

# macOS/Linux
rm elsafa_halls.db
python run.py
```

### المشكلة: الواجهة تبدو غريبة / مشاكل في التصميم
**الحل:** امسح ذاكرة التخزين المؤقتة للمتصفح:
- اضغط `Ctrl+Shift+Delete` (أو Cmd+Shift+Delete على Mac)
- اختر حذف كل البيانات

---

## الاستخدام الأساسي / Basic Usage

### 1. تسجيل الدخول / Login
1. ادخل `admin` و `admin123` أو بيانات موظف
2. ستُفتح لوحة التحكم الخاصة بك

### 2. إضافة قاعة جديدة (مدير فقط)
1. اذهب إلى "الإدارة" → "إدارة القاعات"
2. انقر "إضافة قاعة جديدة"
3. ملأ البيانات واحفظ

### 3. إضافة حجز جديد (موظف)
1. انقر "حجز جديد" من لوحة التحكم
2. اختر القاعة والتاريخ والأوقات
3. أدخل بيانات العميل
4. احفظ الحجز

### 4. إدارة الحجوزات (مدير)
1. اذهب إلى "جميع الحجوزات"
2. بحث أو تصفية حسب المعايير
3. صرّح أو ارفض الحجوزات المعلقة
4. عدّل أو احذف الحجوزات

---

## الواجهات البرمجية / API Endpoints

### الحصول على تقويم شهر
```
GET /api/calendar?month=2025-01
```

### الحصول على الأوقات المتاحة
```
GET /api/availability?hall_id=1&date=2025-01-15
```

---

## تخصيص التطبيق / Customization

### تغيير اسم الشركة
عدّل `base.html`:
```html
<a class="navbar-brand" href="...">
    <i class="fas fa-building"></i> اسمك
</a>
```

### تغيير الألوان
عدّل `static/css/style.css`:
```css
:root {
    --primary-color: #0d6efd;  /* اللون الأساسي */
}
```

### إضافة حقول جديدة
1. عدّل `models.py`
2. عدّل القوالب المرتبطة
3. حذف قاعدة البيانات القديمة وابدأ من جديد

---

## الأمان / Security

⚠️ **تنبيهات أمنية:**

1. **غيّر كلمات المرور الافتراضية** قبل الإطلاق
2. **لا تشارك** ملف قاعدة البيانات
3. **استخدم HTTPS** في الإنتاج
4. **قيّد الوصول** إلى شبكة محلية فقط

---

## الدعم والمساعدة / Support

إذا واجهت مشاكل:
1. تحقق من الـ Console لرؤية الأخطاء
2. تأكد من تثبيت كل المكتبات
3. حاول إعادة تشغيل التطبيق

---

## الخطوات التالية / Next Steps

بعد التشغيل الناجح:
1. استكشف جميع المميزات
2. أضف القاعات الفعلية
3. أضف موظفيك الحقيقيين
4. ابدأ استقبال الحجوزات!

---

**استمتع باستخدام نظام الحجوزات! Enjoy using Safa Downtown Halls!** 🎉

آخر تحديث / Last Updated: يناير 2025
